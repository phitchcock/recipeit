<h1>Categories</h1>

<% @categories.each do |category| %>
  <p><%= link_to category.name, category_path(category) %> | <%= link_to 'Edit', edit_category_path(category) %></p>
<% end %>

<%= link_to "New", new_category_path %>

<div>
  <%= will_paginate @categories %>
</div>

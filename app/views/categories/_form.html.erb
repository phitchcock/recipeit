<%= form_for(@category) do |f| %>
  <%= render 'shared/errors', obj: @category %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
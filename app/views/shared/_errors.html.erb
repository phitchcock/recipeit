<% if obj.errors.any? %>
  <div>
    <h2><%= pluralize(obj.errors.count , "error") %> prevented save</h2>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>